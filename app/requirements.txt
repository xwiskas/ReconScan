# ReconScan backend dependencies (Python 3.11+)
fastapi==0.115.6
uvicorn[standard]==0.34.0
argon2-cffi==23.1.0        # secure password hashing for LAN login
python-multipart==0.0.20   # form/file uploads (target import)
cryptography==44.0.0       # self-signed certificate for LAN HTTPS
